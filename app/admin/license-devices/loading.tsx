import { AdminListLoading } from "@/components/admin/admin-skeleton"

export default function DevicesLoading() {
  return <AdminListLoading title="License Devices" description="Loading registered installs." tableTitle="Registered installs" columns={7} />
}
