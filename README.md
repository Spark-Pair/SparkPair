# 🚀 SparkPair — Next.js Application

This is a modern web application built using **Next.js**, **TypeScript**, and a component-based architecture.  
The project follows a clean folder structure with reusable components, hooks, and modular libraries.

---

## 📁 Project Structure

